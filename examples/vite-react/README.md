# Vite example

This example is scaffolded with `npm create vite@latest` and wired to the local `better-react-transliterate` package through the workspace.

## Run

From the repository root:

```bash
pnpm install
pnpm build:library
pnpm dev
```
